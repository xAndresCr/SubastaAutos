@model IEnumerable<SubastaAutos.Application.DTOs.SubastaDTO>

@{
    ViewData["Title"] = "Subastas Activas";
    var total = Model?.Count() ?? 0;
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h3 mb-1" data-i18n="subasta.activeTitle">Subastas Activas</h1>
        <p class="text-muted mb-0">
            <span data-i18n="common.total">Total</span>: <strong>@total</strong>
        </p>
    </div>
</div>

@if (Model == null || !Model.Any())
{
    <div class="alert alert-info" data-i18n="subasta.noActive">
        No hay subastas activas en este momento.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 row-cols-xl-4 g-4">
        @foreach (var item in Model)
        {
            <div class="col">
                <article class="card h-100 shadow-sm border-0">

                
                    <div class="overflow-hidden bg-body-tertiary rounded-top"
                         style="height:200px;">
                        @if (!string.IsNullOrWhiteSpace(item.ImagenPrincipalAuto))
                        {
                            <img class="w-100 h-100 object-fit-cover"
                                 src="@item.ImagenPrincipalAuto"
                                 alt="@item.NombreAuto"
                                 loading="lazy" />
                        }
                        else
                        {
                            <div class="d-flex flex-column justify-content-center
                                        align-items-center h-100 text-muted">
                                <i class="bi bi-car-front fs-1"></i>
                                <span class="small" data-i18n="common.noImage">Sin imagen</span>
                            </div>
                        }
                    </div>

                    <div class="card-body">
                        <h2 class="h6 fw-semibold mb-2 text-truncate"
                            title="@item.NombreAuto">
                            @item.NombreAuto
                        </h2>

                        <p class="fw-bold text-success mb-2">
                            @item.PrecioBase.ToString("C", new System.Globalization.CultureInfo("es-CR"))
                        </p>

                        <div class="d-flex flex-wrap gap-1 mb-2">
                            <span class="badge text-bg-success">
                                <i class="bi bi-circle-fill me-1" style="font-size:.45rem;"></i>
                                @item.EstadoSubasta
                            </span>

                            <span class="badge text-bg-warning">
                                <i class="bi bi-hammer me-1"></i>@item.CantidadPujas <span data-i18n="subasta.bids">pujas</span>
                            </span>
                        </div>

                        <small class="text-muted d-block">
                            <i class="bi bi-calendar-event me-1"></i>
                            <span data-i18n="subasta.closes">Cierra</span>: @item.FechaCierre.ToString("dd/MM/yyyy HH:mm")
                        </small>
                    </div>

                    <div class="card-footer bg-body border-0 pb-3">
                        <div class="d-grid">
                            <a asp-action="Details" asp-route-id="@item.IdSubasta"
                               class="btn btn-secondary btn-sm">
                                <i class="bi bi-eye me-1"></i><span data-i18n="common.viewDetail">Ver detalle</span>
                            </a>
                        </div>
                    </div>

                </article>
            </div>
        }
    </div>
}

@section Scripts {
    <style>
        .card:hover { transform: translateY(-3px); transition: transform .18s ease; }
    </style>
}
